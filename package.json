{
  "name": "firevue-reactive",
  "version": "0.0.1",
  "description": "A Firestore + Vue Library to link Firestore docs/queries to Vue 3 Composition API observables",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "devDependencies": {
    "gts": "^2.0.2",
    "typescript": "^3.8.3",
    "@types/node": "^13.11.1",
    "@vue/composition-api": "^0.6.5",
    "firebase": "^7.15.3",
    "vue": "^2.6.11"
  },
  "peerDependencies": {
    "@vue/composition-api": ">=0.6.4",
    "firebase": ">=7.15.0",
    "vue": ">=2.6.11"
  }
}
